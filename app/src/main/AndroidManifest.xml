<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- 定义命名空间：android 和 tools -->

    <!-- 声明应用使用的硬件功能，telephony 表示电话功能，但不是必需的 -->
    <uses-feature
        android:name="android.hardware.telephony"
        android:required="false" />

    <!-- 添加权限声明 -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/aaaa"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/aaaa"
        android:supportsRtl="true"
        android:theme="@style/Theme.CA_test"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">

        <!-- 定义应用程序属性 -->
        <!-- android:allowBackup="true" 表示允许应用数据备份 -->
        <!-- android:dataExtractionRules="@xml/data_extraction_rules" 指定数据提取规则文件 -->
        <!-- android:fullBackupContent="@xml/backup_rules" 指定完全备份内容文件 -->
        <!-- android:icon="@mipmap/ic_launcher" 设置应用图标 -->
        <!-- android:label="@string/app_name" 设置应用名称 -->
        <!-- android:roundIcon="@mipmap/ic_launcher_round" 设置圆形图标 -->
        <!-- android:supportsRtl="true" 表示支持从右到左的布局 -->
        <!-- android:theme="@style/Theme.CA_test" 设置应用主题 -->
        <!-- android:networkSecurityConfig="@xml/network_security_config" 引用网络安全配置文件 -->
        <!-- tools:targetApi="31" 指定目标 API 级别 -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <!-- 声明 MainActivity -->
            <!-- android:name=".MainActivity" 指定活动的类名 -->
            <!-- android:exported="true" 表示该活动可以由其他应用启动 -->
            <!-- android:screenOrientation="portrait" 强制活动以竖屏模式显示 -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <!-- 声明活动的主要操作 -->
                <category android:name="android.intent.category.LAUNCHER" />
                <!-- 声明活动是启动器中的主要入口点 -->
            </intent-filter>
        </activity>

        <activity
            android:name=".VideoActivity"
            android:screenOrientation="portrait">
            <!-- 声明 VideoActivity -->
            <!-- android:name=".VideoActivity" 指定活动的类名 -->
            <!-- android:screenOrientation="portrait" 强制活动以竖屏模式显示 -->
        </activity>
    </application>
</manifest>
